<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>{{ site_url }}/zh/</loc></url>
  <url><loc>{{ site_url }}/en/</loc></url>
  <url><loc>{{ site_url }}/zh/articles</loc></url>
  <url><loc>{{ site_url }}/en/articles</loc></url>
  <url><loc>{{ site_url }}/zh/videos</loc></url>
  <url><loc>{{ site_url }}/en/videos</loc></url>
  <url><loc>{{ site_url }}/zh/about</loc></url>
  <url><loc>{{ site_url }}/en/about</loc></url>
  {% for article in articles %}
  <url>
    <loc>{{ site_url }}/{{ article.language }}/articles/{{ article.slug }}</loc>
    {% if article.created_at %}<lastmod>{{ article.created_at[:10] }}</lastmod>{% endif %}
  </url>
  {% endfor %}
</urlset>
